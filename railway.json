{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm exec turbo run build --filter=@provia/api..."
  },
  "deploy": {
    "startCommand": "pnpm --filter @provia/api start"
  }
}
